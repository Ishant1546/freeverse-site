<% include('../layouts/base.ejs') %>
<% layout('layouts/base', { pageTitle: 'Hosting Plans' }) %>
<% body = `
  <section>
    <h2 class="text-3xl font-bold mb-3">Hosting Plans</h2>
    <p class="small-muted mb-6">${plans.low.note}</p>

    <h3 class="text-2xl font-semibold mb-4">${plans.low.title}</h3>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
      ${plans.low.plans.map(p => `
        <div class="card">
          <h4 class="text-lg font-bold">${p.name} — <span class="text-fvgold">${p.price}</span></h4>
          <ul class="mt-3 small-muted space-y-1">
            <li>💾 ${p.ram}</li>
            <li>💽 ${p.disk}</li>
            <li>🖥 ${p.cpu}</li>
            ${p.extras.map(e => `<li>${e}</li>`).join('')}
          </ul>
        </div>
      `).join('')}
    </div>

    <p class="small-muted mb-6">${plans.high.note}</p>
    <h3 class="text-2xl font-semibold mb-4">${plans.high.title}</h3>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      ${plans.high.plans.map(p => `
        <div class="card">
          <h4 class="text-lg font-bold">${p.name} — <span class="text-fvgold">${p.price}</span></h4>
          <ul class="mt-3 small-muted space-y-1">
            <li>💾 ${p.ram}</li>
            <li>💽 ${p.disk}</li>
            <li>🖥 ${p.cpu}</li>
            ${p.extras.map(e => `<li>${e}</li>`).join('')}
          </ul>
        </div>
      `).join('')}
    </div>
  </section>
` %>
<%- body %>
